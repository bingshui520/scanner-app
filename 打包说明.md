# 📦 APK 打包说明

## 🎯 三种打包方式

### 方式一：一键打包脚本（推荐）

双击运行 `build_apk.bat`，脚本会自动：
1. 检查开发环境
2. 执行编译打包
3. 显示 APK 位置

**APK 输出位置**：
```
app\build\outputs\apk\debug\app-debug.apk
```

---

### 方式二：Android Studio（最稳定）

#### 步骤：

1. **打开项目**
   ```
   Android Studio -> File -> Open
   -> 选择: d:\skills\saolei
   ```

2. **等待同步**
   - Gradle 会自动下载依赖
   - 首次同步可能需要 3-10 分钟
   - 确保网络畅通

3. **编译 APK**
   ```
   Build -> Build Bundle(s) / APK(s) -> Build APK(s)
   ```
   或按快捷键：`Ctrl + Shift + A` 输入 "Build APK"

4. **等待完成**
   - 底部会显示编译进度
   - 完成后会弹出提示

5. **查找 APK**
   - 点击提示中的 "locate" 链接
   - 或手动进入：`app\build\outputs\apk\debug\`
   - 文件名：`app-debug.apk`

---

### 方式三：命令行（需要配置环境）

#### 前提条件：
- 已安装 Android SDK
- 已配置 ANDROID_HOME 环境变量
- 已安装 JDK 8+

#### 命令：

```bash
# 进入项目目录
cd d:\skills\saolei

# 赋予执行权限（Linux/Mac）
chmod +x gradlew

# 编译 Debug 版本
gradlew.bat assembleDebug

# 或编译 Release 版本（需要签名）
gradlew.bat assembleRelease
```

---

## 📱 安装 APK

### 方法 1：USB 连接

1. 手机开启 USB 调试
2. 连接电脑
3. 在 Android Studio 中点击 Run (▶️)
4. 或使用命令：
   ```bash
   adb install app\build\outputs\apk\debug\app-debug.apk
   ```

### 方法 2：直接安装

1. 将 `app-debug.apk` 复制到手机
2. 在手机上点击 APK 文件
3. 允许"未知来源"安装
4. 点击安装

### 方法 3：扫码安装

1. 将 APK 上传到云端
2. 生成下载二维码
3. 手机扫码下载安装

---

## ⚠️ 常见问题

### Q1: Gradle 同步失败？
**A:** 
- 检查网络连接
- 配置 Gradle 镜像（使用阿里云镜像）
- 在 `build.gradle` 中添加：
  ```gradle
  repositories {
      maven { url 'https://maven.aliyun.com/repository/public/' }
      maven { url 'https://maven.aliyun.com/repository/google/' }
  }
  ```

### Q2: 编译失败？
**A:**
- 检查 Android SDK 是否完整
- 确保安装了 API 33
- Tools -> SDK Manager -> 安装缺失的组件

### Q3: 无法安装到手机？
**A:**
- 允许"未知来源"安装
- 检查是否被安全软件拦截
- 尝试使用 ADB 安装

### Q4: APK 太大？
**A:**
- Debug 版本约 5-10 MB（包含调试信息）
- Release 版本会更小（经过混淆和压缩）
- 可以启用 ProGuard 进一步压缩

---

## 🔐 签名 APK（可选）

如果要发布到应用商店，需要签名：

1. **生成密钥**
   ```bash
   keytool -genkey -v -keystore scanner.jks -keyalg RSA -keysize 2048 -validity 10000 -alias scanner
   ```

2. **配置签名**
   在 `app/build.gradle` 中添加：
   ```gradle
   android {
       signingConfigs {
           release {
               storeFile file("scanner.jks")
               storePassword "your_password"
               keyAlias "scanner"
               keyPassword "your_password"
           }
       }
       buildTypes {
           release {
               signingConfig signingConfigs.release
           }
       }
   }
   ```

3. **编译签名版本**
   ```bash
   gradlew.bat assembleRelease
   ```

---

## 📊 APK 信息

- **包名**: com.scanner.app
- **版本**: 1.0 (versionCode: 1)
- **最低支持**: Android 5.0 (API 21)
- **目标版本**: Android 13 (API 33)
- **大小**: 约 5-10 MB (Debug)

---

## 🎯 登录信息

安装后使用以下账号登录：

- **用户名**: `admin`
- **密码**: `admin`

---

## 📞 技术支持

如有问题，请检查：
1. Android Studio 版本（建议 4.0+）
2. Gradle 版本（7.5）
3. JDK 版本（建议 JDK 11）
4. Android SDK 是否完整

---

**祝打包顺利！** 🎉
